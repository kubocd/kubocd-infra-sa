---
apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: OCIRepository
metadata:
  name: kubocd-controller
  namespace: flux-system
spec:
  interval: 1m
  layerSelector:
    mediaType: "application/vnd.cncf.helm.chart.content.v1.tar+gzip"
    operation: copy
  url: oci://ghcr.io/sergealexandre/kubocd/charts/kubocd
  ref:
    semver: "= v0.1.1-snapshot"


---
apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: kubocd-controller
  namespace: flux-system
spec:
  interval: 1m
  serviceAccountName: kustomize-controller
  targetNamespace: kubocd
  storageNamespace: flux-system
  releaseName: kubocd-ctrl
  timeout: 5m
  install:
    createNamespace: true
    remediation:
      retries: 30
      remediateLastFailure: true
  upgrade:
    remediation:
      retries: 30
      remediateLastFailure: true
  chartRef:
    kind: OCIRepository
    name: kubocd-controller
    namespace: flux-system
  values:
    image:
      repository: ghcr.io/sergealexandre/kubocd
      pullPolicy: Always
    extraNamespaces:
      - name: contexts
    contexts:
      - name: global
        namespace: contexts
        parents: []
        context:
          # This is for cert-manager's users
          certificateIssuer:
            public: cluster-odp
            internal: cluster-internal
        protected: true
      - name: kind
        namespace: contexts
        description: For all kind based clusters
        parents:
          - name: global
        context:
          storageClass:
            data: standard
            workspace: standard
          ingress:
            className: nginx
        protected: true
      - name: cluster
        namespace: contexts
        description: Context specific to the cluster 'kubo1'
        parents:
          - name: kind
        context:
          cluster:
            name: kubo1
            domain: kubo1.mbp
          ingress:
            hostPostfix: ingress.kubo1.mbp
            kubernetes:
              enabled: true
        protected: true
    config:
      kuboAppRedirects:
        - oldPrefix: quay.io/kubocd/
          newPrefix: ghcr.io/sergealexandre/kubocd/
        # Other OciRepositorySpec elements
      clusterRoles:
        - storage
      imageRedirects: []
      defaultContexts:
        - name: cluster
          namespace: contexts
    controller:
      enabled: true
      replicaCount: 1
      logger:
        mode: dev
        level: debug
      metrics:
        enabled: false
        secured: false
    webhook:
      enabled: false

